{
    Defaults = {
        ExecutableExtensions = (app, debug);
        LoadableExtensions = (bundle, framework, palette);
        SenTest = All;
        TestRunnerPath = "/usr/local/bin/otest";
    };
}